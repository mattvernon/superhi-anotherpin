<h2><%= @product.title %></h2>

<%= simple_format @product.description %>

<%= simple_form_for [@product, @order_item] do |f| %>
  <%= f.input :quantity %>
  <%= f.button :submit, "Add to Cart" %>
<% end %>



<%= image_tag @product.image_1 %>

<p>
  <%= link_to "Back to Home", products_path %>
</p>
